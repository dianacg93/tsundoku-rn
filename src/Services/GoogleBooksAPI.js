import React from "react";
import { StyleSheet, Text, View } from "react-native";

const GoogleBooksAPI = () => {
	return (
		<View>
			<Text>Google Books API data</Text>
		</View>
	);
};

export default GoogleBooksAPI;

const styles = StyleSheet.create({});
