import React from "react";
import { StyleSheet, Text, View } from "react-native";

const LocalShopsSources = () => {
	return (
		<View>
			<Text>Local Shop Sources</Text>
		</View>
	);
};

export default LocalShopsSources;

const styles = StyleSheet.create({});
