import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SearchBooks = () => {
	return (
		<View>
			<Text>Search Books Screen</Text>
		</View>
	);
};

export default SearchBooks;

const styles = StyleSheet.create({});
