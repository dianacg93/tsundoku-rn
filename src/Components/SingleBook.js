import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SingleBook = () => {
	return (
		<View>
			<Text>Single Book Component</Text>
		</View>
	);
};

export default SingleBook;

const styles = StyleSheet.create({});
