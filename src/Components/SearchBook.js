import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SearchBook = () => {
	return (
		<View>
			<Text>Search Book (button?) component</Text>
		</View>
	);
};

export default SearchBook;

const styles = StyleSheet.create({});
