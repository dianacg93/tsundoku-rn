import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Books = () => {
	return (
		<View>
			<Text>Map Books Component</Text>
		</View>
	);
};

export default Books;

const styles = StyleSheet.create({});
